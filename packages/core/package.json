{
  "name": "@monorepo/core",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "files": [
    "dist",
    "package.json"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "clean": "rm -rf ./dist",
    "prebuild": "yarn clean",
    "build": "tsc --project tsconfig.app.json",
    "build:watch": "yarn build --watch"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "18.15.0",
    "typescript": "^5.1.6"
  }
}
